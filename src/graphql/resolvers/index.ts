import CommentResolver from "./commentResolver";
import BlogResolver from "./blogResolver";
import UserResolver from "./userResolver";

const resolvers = [UserResolver, BlogResolver, CommentResolver];

export default resolvers;
